imports:
- path: portainer-cluster.jinja

resources:
- name: portainer-cluster
  type: portainer-cluster.jinja
  properties:
    instances:
    - name: portainer
      zone: us-east1-b
      machineType: f1-micro
      diskSizeGB: 20
      tags: ["portainer"]
      os-image: projects/cos-cloud/global/images/cos-77-12371-1109-0
      startup-script: |
        #!/bin/bash
        docker volume create portainer_data;
        docker run -d -p 8000:8000 -p 9000:9000 --name=portainer --restart=always -v /var/run/docker.sock:/var/run/docker.sock -v portainer_data:/data portainer/portainer-ce;
    - name: nextcloud
      zone: us-east1-b
      machineType: f1-micro
      diskSizeGB: 500
      tags: ["nextcloud"]
      os-image: projects/cos-cloud/global/images/cos-77-12371-1109-0
      startup-script: |
        #!/bin/bash
        docker volume create nextcloud-data;
        docker run -d --restart=always --name=nextcloud -p 80:80 -v nextcloud-data:/var/www/html nextcloud:latest;
    - name: qiime
      zone: us-central1-f
      machineType: e2-standard-8
      diskSizeGB: 500
      os-image: projects/debian-cloud/global/images/debian-9-stretch-v20210316
      startup-script: |
        #!/bin/bash
        apt update;
        apt install apt-transport-https ca-certificates curl gnupg2 software-properties-common -y;
        curl -fsSL https://download.docker.com/linux/debian/gpg | apt-key add -;
        add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/debian $(lsb_release -cs) stable";
        apt update;
        apt install docker-ce -y;
        docker plugin install --grant-all-permissions fentas/davfs;
        docker volume create qiime-workspace;